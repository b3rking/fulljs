while(condition) {
    // code a executer
}

// pour un code d'une seule ligne les accolades sont pas requise

do {
    // code a executer
} while(condition)

for (debut, condition, etape) {
    // code a executer
}

