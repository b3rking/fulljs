// convertion de type

// 1. anytype to string convertion

let text = String(15)   // converting number to string
console.log(typeof(text)); // returns string

// 2. string number to number

let num = Number('45')  // converting text to number
console.log(typeof(num));   // returns number

// 3. boolean conversion

console.log(Boolean(1))    // returns true
console.log(Boolean(0))    // returns false
